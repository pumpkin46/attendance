# AI service + Backend dependencies (Python 3.10+; ML pins below need 3.12+ wheels on 3.14)

# Web framework
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.1

# Database
sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0
alembic==1.18.4
greenlet==3.5.1

# Auth & security
python-jose[cryptography]==3.5.0
passlib[argon2]==1.7.4
argon2-cffi==25.1.0
bcrypt==5.0.0

# AI / ML (numpy>=2.3.2 / onnxruntime>=1.24 / sklearn>=1.9 / asyncpg>=0.31 for cp314 wheels)
numpy==2.4.6
opencv-python-headless==4.13.0.92
faiss-cpu==1.14.2
insightface==1.0.1
onnxruntime==1.26.0
Pillow==12.2.0
scikit-learn==1.9.0
# Cache, broker & background tasks
# redis is capped <6.5 by kombu (celery's broker lib) — do NOT bump to 8.x until
# celery/kombu widen the pin, or `celery[redis]` resolution will fail.
redis==6.4.0
celery[redis]==5.6.3

# Utilities
httpx==0.28.1
python-dateutil==2.9.0.post0

#Unit test
pytest==9.0.3
pytest-asyncio==1.4.0
